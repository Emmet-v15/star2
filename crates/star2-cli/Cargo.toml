[package]
name = "star2-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

# The engine is the supervised child; `star2` itself is the updater/runner.
[[bin]]
name = "star2-engine"
path = "src/main.rs"

[dependencies]
star2-engine = { path = "../star2-engine" }
anyhow = { workspace = true }
cpal = "0.15"
